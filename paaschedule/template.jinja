<!DOCTYPE html>
<html>
<head>
  <title>Daily Time Calendar</title>
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }
    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: center;
    }
    th {
      background-color: #f2f2f2;
    }
    td.time-slot {
      font-weight: bold;
    }
  </style>
</head>
<body>

<table>
  <tr>
    <th>Time</th>
    {% for date in dates %}
    <th>{{ date }}</th>
    {% endfor %}
  </tr>
  
  {% for time_slot in time_slots %}
  <tr>
    <td class="time-slot">{{ time_slot }}</td>
    {% for _ in dates %}
    <td></td>
    {% endfor %}
  </tr>
  {% endfor %}

</table>

</body>
</html>
